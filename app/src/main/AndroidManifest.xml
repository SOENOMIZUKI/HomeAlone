<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK"/>

    <application
        android:networkSecurityConfig="@xml/network_security_config"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >

        <activity android:name=".Ledgerchange">
        </activity>

        <activity android:name=".Ledgerinput">
        </activity>

        <activity android:name=".Ledgerdisp" >
        </activity>

        <activity android:name=".Signup" android:label="アカウント作成">
        </activity>

        <activity android:name=".alarm_wakeupActivity">
        </activity>

        <activity android:name=".HeaderActivity">
        </activity>

        <activity android:name=".MapActivity">
        </activity>

        <activity android:name=".WeatherActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity">
        </activity>

        <activity android:name=".CalendarActivity">
        </activity>
        <activity android:name=".AlarmActivity" >
        </activity>

        <activity android:name=".AlarmSettingActivity">
        </activity>

        <activity android:name=".AlarmSettingChangeActivity">
        </activity>

        <activity android:name=".CalendarComfirmActivity">
        </activity>

        <activity android:name=".CalendarAddActivity">
        </activity>

        <receiver android:name=".AlarmReceiver" android:process=":remote" />

        <service
            android:name=".SoundService"
            android:enabled="true"
            android:exported="false" />

    </application>
</manifest>